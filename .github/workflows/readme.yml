---
name: readme

"on":
  push:
    branches:
      - master
    paths:
      - README.md
  workflow_dispatch:

jobs:
  readme:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6

      - name: Push readme
        uses: actionhippie/pushrm@48005862367f0dae301f13a06a2b0e4458ec582c # v1
        if: github.event_name != 'pull_request'
        with:
          provider: dockerhub
          target: webhippie/sentry
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          description: Docker image for sentry
          readme: README.md

      - name: Push readme
        uses: actionhippie/pushrm@48005862367f0dae301f13a06a2b0e4458ec582c # v1
        with:
          provider: quay
          target: quay.io/webhippie/sentry
          apikey: ${{ secrets.QUAY_APIKEY }}
          readme: README.md

...
